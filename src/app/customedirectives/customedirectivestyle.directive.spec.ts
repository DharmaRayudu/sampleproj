import { CustomedirectivestyleDirective } from './customedirectivestyle.directive';

describe('CustomedirectivestyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomedirectivestyleDirective();
    expect(directive).toBeTruthy();
  });
});
