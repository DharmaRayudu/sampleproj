import { IfCustomDirectiveDirective } from './if-custom-directive.directive';

describe('IfCustomDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IfCustomDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
